<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Wild-Cartoons !{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <header>
        <div class="header-logo"><a href="{{ path('home') }}">Wild Cartoons</a></div>
        <nav>
            <ul>
                <li><a href="{{ path('program_index') }}">Cartoons</a></li>
                <li>Favoris</li>
            </ul>
        </nav>
        <div class="header-form-links">
            <p>Connection</p>
        </div>
    </header>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
